# pi-observational-memory evals

Model-level evals for observational-memory agents and recall-use behavior.

## Commands

```bash
pnpm om-agent-evals
pnpm recall-use-evals
```

Real and giga session-derived cases are kept under `eval/src/om/cases`.
The old generic YAML session replay suites are intentionally not migrated.
